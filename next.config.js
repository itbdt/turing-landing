/**
 * @type {import('next').NextConfig}
 *
 * This configuration enables strict mode for React and leaves
 * the rest of the options at their defaults. You can extend this
 * configuration with custom settings as your application evolves.
 */
const nextConfig = {
  reactStrictMode: true,
};

module.exports = nextConfig;
